# Runtime. Every model runs behind an API, so there is no torch or GPU dependency.

# Web server and HTTP
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
pydantic>=2.6
python-dotenv>=1.0

# The statute corpus: LanceDB, with pandas for its query results
lancedb>=0.13
pandas>=2.1
numpy>=1.26

# Reading the web
crawl4ai>=0.9
ddgs>=9.0

# Exact token counts for prompt budgets (a character heuristic is used without it)
tiktoken>=0.7
